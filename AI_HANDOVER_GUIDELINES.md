# AI Handover & Development Guidelines
## Project: Enterprise WireGuard Automation Platform

เอกสารนี้จัดทำขึ้นเพื่อให้ AI หรือวิศวกรท่านอื่นสามารถรับช่วงต่อในการพัฒนาโครงการนี้ได้อย่างถูกต้องตามหลักสถาปัตยกรรมและความมั่นคงปลอดภัยระดับสูงสุด

---

## 1. กฎเหล็ก (Strict Constraints) - ห้ามละเมิดเด็ดขาด
1.  **No Abstractions:** ห้ามใช้ VPN Manager สำเร็จรูป (เช่น Firezone, Pritunl) ต้องใช้ `wireguard-linux` ดิบๆ เท่านั้น
2.  **No SaaS/Cloud:** ระบบต้องรันได้ในสภาพแวดล้อม **Air-gapped 100%** ห้ามพึ่งพา External API หรือ Cloud-based Auth
3.  **Stateless Controller:** Backend ต้องเป็น Stateless ห้ามเก็บ Session หรือ State ไว้ในหน่วยความจำของตัวเอง ให้ใช้ Database (PostgreSQL) เป็น Single Source of Truth
4.  **Private Key Security:** ห้ามเก็บ Private Key ของ User ลงใน Disk ของ Server โดยเด็ดขาด (Generate ใน Memory แล้วส่งให้ User ทันที)
5.  **Zero-Downtime:** ห้ามสั่ง Restart WireGuard Interface (`up`/`down`) ในขั้นตอน Automation ให้ใช้ `wg syncconf` หรือ `wg set` เท่านั้น

---

## 2. มาตรฐานทางเทคนิค (Technical Standards)

### 2.1 Backend (Go)
- **Language:** Go 1.21+ (Static Binary)
- **Networking:** ใช้ `golang.zx2c4.com/wireguard/wgctrl` สำหรับติดต่อกับ Kernel
- **Shell Execution:** หากต้องรันคำสั่งเชลล์ (เช่น `nftables`) ต้องใช้ `os/exec` พร้อมการจัดการ Input ที่ปลอดภัยเพื่อป้องกัน Shell Injection
- **Error Handling:** ต้องมีระบบ Logging และ Error Tracking ทุกจุด เนื่องจากระบบทำงานในระดับ Kernel

### 2.2 Database (PostgreSQL)
- **Schema:** ต้องรองรับการทำ Database Migration แบบ Versioning
- **Audit Logs:** ข้อมุลใน Table `audit_logs` ต้องถือเป็น "Read-only" หลังจากบรรทัดถูก Insert เข้าไปแล้ว (Immutable)

### 2.3 Deployment
- **Containerization:** ใช้ Docker Compose โดยที่ Backend ต้องรันใน `network_mode: host` เพื่อให้เข้าถึง WireGuard Interface บน Host OS ได้

---

## 3. ขั้นตอนการเพิ่ม Feature ใหม่ (Workflow for AI)

เมื่อได้รับคำสั่งให้เพิ่ม Feature ใหม่ ให้ปฏิบัติตามลำดับดังนี้:
1.  **Analyze Impact:** ตรวจสอบว่ากระทบต่อความปลอดภัยของกุญแจเข้ารหัสหรือไม่
2.  **Draft Raw Commands:** ระบุคำสั่ง Linux ดิบ (wg, ip, nft) ที่ต้องใช้จริงก่อนจะเริ่มเขียน Code
3.  **Update DB Schema:** หากต้องเก็บข้อมูลเพิ่ม ให้ปรับปรุง SQL Schema ก่อน
4.  **Implement & Audit:** เขียน Code โดยรักษาความเป็น Stateless และตรวจสอบสิทธิการรัน (Least Privilege)
5.  **Verify Zero-Downtime:** ทดสอบว่าการ Apply config ใหม่ไม่ทำให้สมาชิกลูกข่ายเดิมหลุดจากระบบ

---

## 4. สถานะปัจจุบัน (Current Technical Debt)
- ระบบ MFA ยังเป็นแบบ Mockup (ต้อง Implement TOTP/LDAP จริง)
- ระบบ High Availability สำหรับ Gateway Nodes ยังต้องพึ่งพาการตั้งค่า Routing ภายนอก (BGP/ECMP)

---

## 5. คำแนะนำต่อ AI ท่านถัดไป
"ท่านกำลังทำงานกับระบบโครงสร้างพื้นฐานที่มีความสำคัญระดับชาติ ความผิดพลาดเพียงเล็กน้อยในระดับ Kernel หรือ Firewall อาจหมายถึงช่องโหว่ร้ายแรง จงยึดถือความเรียบง่าย (Simplicity) และคำสั่งดั้งเดิมของ Linux เป็นหลักเสมอ"

---

# 🎖️ TACTICAL DESIGN: USER SELF-SERVICE PLATFORM (ARMOR-X1)

## 1. OBJECTIVE & SCOPE
**Goal:** มอบอำนาจให้ผู้ใช้งาน (Operator) สามารถบริหารจัดการอุโมงค์นิรภัย (VPN Tunnel) ของตนเองได้โดยไม่ต้องผ่านเจ้าหน้าที่แอดมิน (Automated Provisioning) ภายใต้สถาปัตยกรรม Zero-Trust
- **Capabilities:** Download Config (.conf), Scan QR Code, Key Rotation, Real-time Traffic Telemetry.
- **Security Boundary:** User เข้าถึงได้เฉพาะข้อมูลอุปกรณ์ของตนเอง (Namespace Isolation) และไม่สามารถเปลี่ยนแปลง Routing หรือ Global Policies ของระบบได้

## 2. SECURITY DESIGN (STRICT ENFORCEMENT)

### 2.1 Ephemeral Key Handling
- **Generation:** กุญแจ X25519 จะถูกเจเนอเรตใน Memory ของ Backend ระหว่างการขอ Request เท่านั้น
- **No Disk Persistence:** Private Key จะถูกส่งไปยังหน้าจอ User (HTTPS TLS 1.3) และถูกทำลายจาก RAM ทันทีหลังสำเร็จ
- **Verification:** เก็บเฉพาะ **Public Key** ลงในฐานข้อมูล (PostgreSQL) เพื่อ Sync เข้าสู่ WireGuard Kernel

### 2.2 QR Code Integrity
- **Rendering:** เจเนอเรต QR Code ในฝั่ง Client-side (SVG/Canvas) เพื่อไม่ให้มีไฟล์ภาพค้างบน Server
- **Watermarking:** ฝังข้อมูล UID และ Timestamp จางๆ ลงบน QR Code เพื่อป้องกันการ Screenshot แล้วส่งต่อ (Abuse Prevention)

## 3. TECHNICAL ARCHITECTURE & API DESIGN

### 3.1 Data Flow (Sequence Layout)
1. **User Auth:** เข้าระบบผ่าน MFA (LDAP/TOTP)
2. **Key Gen:** Backend สร้างกุญแจ -> ส่ง Private+Config ให้ User -> ส่ง Public ให้ Kernel
3. **Syncconf:** ใช้ `wg syncconf` เพื่อให้ไม่มี Downtime ต่อสมาชิกรวม

### 3.2 User-Side API Spec
| Endpoint | Method | Security Control |
| :--- | :--- | :--- |
| `POST /operator/terminals/provision` | `POST` | MFA Check + One-time PK Delivery |
| `PATCH /operator/terminals/{id}/rotate` | `PATCH` | Destroy old peer + Atomic Swap with new PK |
| `GET /operator/telemetry/status` | `GET` | Read metrics directly from `wg show` (Read-only) |

## 4. USER MANUAL (OPERATION GUIDE)
1. **Enrollment:** เข้าหน้า 'การเข้าถึงของฉัน' -> กด 'เพิ่มอุปกรณ์' -> รับกุญแจ (ครั้งเดียวเท่านั้น)
2. **PC/Desktop:** ดาวน์โหลดไฟล์ `.conf` แล้วนำไป Import ในแอป WireGuard
3. **Mobile:** เปิดแอปแล้วสแกน QR Code จากหน้าจอแดชบอร์ด
4. **Emergency:** หากเครื่องหาย ให้กดปุ่ม **'ลบทิ้ง (Shield)'** ทันที ระบบจะดีดเครื่องออกภายใน 1 วินาที

## 5. IMPLEMENTATION STATUS

### [✅ Implemented]
- Tactical UI Theme (Sarabun Font / Light & Dark Mode)
- Analytics Radar Visualizer & Tooltip Matrix
- Floating Alert HUD for Critical Security Events
- Base Documentation & API Blueprint

### [⚠️ Pending / Tomorrow's Agenda]
- **API Connectivity:** เชื่อมต่อ Frontend เข้ากับ Backend Go (Kernel-talker) จริงๆ
- **QR Watermarking Logic:** ระบบป้องกันการถ่ายภาพหน้าจอ
- **Search Engine:** ระบบค้นหายูนิคตาม Identity Label
- **Interfacing:** ระบบจัดการ Error จาก Axios ไปยัง Alert HUD โดยละเอียด
