# 🛡️ ARMOR-X1: Strategic Mission Manual (v1.2)
**Principal Architect & DevSecOps Engineering Oversight**

ระบบ ARMOR-X1 คือแพลตฟอร์มบริหารจัดการเครือข่าย WireGuard ระดับ Enterprise ที่เน้นความมั่นคงปลอดภัยสูงสุด (Military-Grade) และสถาปัตยกรรมแบบ Zero-Touch Deployment

---

## 🚀 1. Strategic Deployment (การติดตั้ง)

สำหรับการติดตั้งบนเครื่องใหม่ (Ubuntu/Debian) ให้ใช้คำสั่งเดียวเพื่อเริ่มต้นภารกิจ:

```bash
# Clone the Mission Core
git clone <repository_url> armor-matrix
cd armor-matrix

# Execute Unified Installation Script
bash scripts/install.sh
```

### **สิ่งที่ระบบจัดการให้โดยอัตโนมัติ:**
*   **Infrastructure:** ติดตั้ง WireGuard, Nginx, Node.js, และ Go
*   **Networking:** เปิดใช้งาน IP Forwarding และตั้งค่า Nginx Port 80
*   **Persistence:** สร้างระบบ Systemd Service เพื่อให้ระบบฟื้นคืนชีพเองเมื่อเครื่องดับ
*   **Security:** ตั้งค่า Firewall เบื้องต้นผ่าน nftables

---

## 🛠️ 2. Mission Genesis (การเริ่มต้นระบบ)

เมื่อติดตั้งเสร็จ ระบบจะเข้าสู่โหมด **Interactive Setup Wizard** ใน Terminal:

1.  **Organization:** ระบุชื่อหน่วยงาน/องค์กรของคุณ
2.  **Commander Identity:** ตั้งค่า Username และ Password ระดับสูงสุด (Root)
3.  **Deployment Role:** 
    *   **Primary Command Unit:** เครื่องหลักที่ควบคุมโครงข่ายทั้งหมด
    *   **Satellite Edge Unit:** เครื่องลูกที่ทำหน้าที่เป็นจุดรับส่งสัญญาณปลายทาง

---

## 🕹️ 3. Command Hub (การใช้งานหน้าเว็บบอร์ด)

เข้าใช้งานผ่านเบราว์เซอร์ที่พอร์ต **80** (เช่น `http://your-vps-ip`)

### **ฟีเจอร์หลักระดับกลยุทธ์:**
*   **Identity Matrix:** บริหารจัดการบัญชีผู้ใช้และสิทธิ์การเข้าถึง
*   **Gateway Matrix:** จัดการโหนด (Node) ทั่วโลก พร้อมแผนที่แบบ Real-time
*   **Strategic Outreach (NEW):**
    *   **DNS Filtering:** เปิดใช้งาน Pi-hole เพื่อกรองโฆษณาและเว็บอันตราย
    *   **Stealth APN:** ปรับแต่งการเจาะสัญญาณเครือข่ายที่ไม่มีอินเทอร์เน็ต
*   **Security Audit:** ตรวจสอบบันทึกการทำงานทุกย่างก้าวในระบบ

---

## 🛡️ 4. Maintenance & Survival (การรักษาความปลอดภัย)

### **กรณีระบบขัดข้อง/เครื่องดับ:**
ระบบจะทำการ **Auto-healing** ตัวเองทันทีที่เครื่องติดขึ้นมา:
*   Backend และ Frontend จะเปิดเอง
*   ค่า NAT และ Firewall ที่ตั้งไว้จะถูกเขียนลง Kernel ใหม่อัตโนมัติ

### **คำสั่งควบคุมที่สำคัญ (CLI):**
*   **เช็คสถานะระบบ:** `sudo systemctl status armor-backend`
*   **ดูบันทึกเหตุการณ์:** `sudo journalctl -u armor-backend -f`
*   **รีสตาร์ทระบบ:** `sudo systemctl restart armor-backend armor-frontend`

---

## 📜 5. Minimalist Design Philosophy
ระบบถูกออกแบบมาให้ไม่มีส่วนเกิน (Redundancy Free):
*   **No PM2:** ใช้ระบบ Native Systemd ประหยัดแรม 100MB+ และเสถียรกว่า
*   **Unified API:** ทุกคำสั่งถูกรวมศูนย์ไว้ที่ Command Unit
*   **Atomic Transactions:** การเปลี่ยนค่า Config จะถูกบันทึกลงทั้งฐานข้อมูลและ Hardware ทันที

---

**MISSION_STATUS: OPERATIONAL**
*ARMOR-X1 - The Art of Secure Connectivity*
